# Functions must be matched before the SPA catch-all, otherwise the /* rule
# rewrites every unmatched request — function calls included — to index.html.
/.netlify/functions/*  /.netlify/functions/:splat  200

/*  /index.html  200
