[build]
  command = "npm run build"
  publish = "dist"
  functions = "netlify/functions"

[functions]
  # esbuild bundles the ESM handler and its deps (nodemailer) correctly.
  node_bundler = "esbuild"

[dev]
  command = "npm run dev"
  targetPort = 5173
